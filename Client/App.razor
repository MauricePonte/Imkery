<CascadingAuthenticationState>
    <Router AppAssembly="@typeof(App).Assembly">
        <Found Context="routeData">
            <AuthorizeRouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)">
                <NotAuthorized>
                    @if (context.User.Identity?.IsAuthenticated != true)
                    {
                        <RedirectToLogin />
                    }
                    else
                    {
                        <p role="alert">@Resources.Resources.Global_UnauthorizedPlaceholder</p>
                    }
                </NotAuthorized>
            </AuthorizeRouteView>
            <FocusOnNavigate RouteData="@routeData" Selector="h1" />
        </Found>
        <NotFound>
            <PageTitle>@Resources.Resources.Global_NotFoundPlaceholder</PageTitle>
            <LayoutView Layout="@typeof(MainLayout)">
                <p role="alert">@Resources.Resources.Global_NothingHerePlaceholder</p>
            </LayoutView>
        </NotFound>
    </Router>
</CascadingAuthenticationState>
