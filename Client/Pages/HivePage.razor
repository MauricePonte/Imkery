@page "/hive"
@page "/hive/{ItemId:guid}"
@using Imkery.Entities

<MudPaper>
    <ObjectForm TItem="Hive" ItemId="@ItemId" ReturnUrl="@(item=> "")">
        <FormContent>
            <MudTextField Label="@Resources.HiveEntity_Identifier" @bind-Value="@context.Identifier" Required="true"/>
        </FormContent>
    </ObjectForm>
</MudPaper>


@code
{
    [Parameter]
    public Guid ItemId { get; set; }
 } 